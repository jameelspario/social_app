<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/sdf">
<include layout="@layout/layout_headback"
android:visibility="gone"
    android:id="@+id/low"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="@dimen/_40sdp"
        android:layout_margin="10dp"
        android:padding="@dimen/_10sdp"
        android:background="@drawable/rectanglepro"
        android:hint="Search..."
        android:visibility="gone"
        android:layout_below="@id/low"
        android:id="@+id/search_users"/>

    <androidx.recyclerview.widget.RecyclerView
        android:background="@color/white"
        android:layout_below="@id/low"
        android:id="@+id/recycler_view"

        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:id="@+id/textnotfind"
        android:textColor="@color/black"

        android:text="No person is followed by you, please follow the favorite one to start chatting."/>
</RelativeLayout>

