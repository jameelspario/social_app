package com.deificindia.indifun.fcm;

import android.app.Activity;
import android.view.View;
import android.widget.Toast;

import com.deificindia.indifun.R;

public class NotificationAlert {

    public void showAlert(Activity activity, String title, String message){
       /* Alerter.create(activity)
                .setTitle(title)
                .setText(message)
                .setIcon(R.drawable.logootp)
                .setIconColorFilter(0) // Optional - Removes white tint
                .setIconSize(R.dimen._24dp) // Optional - default is 38dp
                .setBackgroundColorRes(R.color.colorAccent) // or setBackgroundColorInt(Color.CYAN)
                .setOnClickListener(v-> {
                    Toast.makeText(activity, "OnClick Called", Toast.LENGTH_LONG).show();
                })
                .show();*/


        //Alerter.isShowing()
        //Alerter.hide()
    }
}
